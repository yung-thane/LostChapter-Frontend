import { TrackUserSearches } from './track-user-searches';

describe('TrackUserSearches', () => {
  it('should create an instance', () => {
    expect(new TrackUserSearches()).toBeTruthy();
  });
});
